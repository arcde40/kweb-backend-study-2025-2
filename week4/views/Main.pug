doctype html
html
  head
    meta(charset='utf-8')
    title= title || 'My App'
    link(rel='stylesheet', href='/Main.css')
  body
    h1 사용방법
    h2 /student-info?name={학생이름}
    div 해당 학생이 들었던 강의 및 그 강의에서의 학점을 검색할 수 있습니다.
    h2 /course-info?name={강의이름}
    div 해당 강의에 대한 정보 및 수강생을 조회할 수 있습니다.
    h2 /push-enrollment?studentid={학생id}&courseid={강의id}&gpa={gpa}
    div (학생_id, 강의_id, 학점)에 대한 정보를 새로 삽입합니다.