doctype html
html
  head
    meta(charset="utf-8")
    title 3주차 과제 - 합 / 차 출력하기
    style.
      body {
        font-family: 'Comic Sans MS', 'Arial Rounded MT Bold', cursive;
        background-color: #fff7f7;
        text-align: center;
        padding: 40px;
      }
      form {
        background: #ffffff;
        padding: 25px 30px;
        border-radius: 20px;
        display: inline-block;
        box-shadow: 0 0 15px rgba(255,182,193,0.3);
        border: 2px solid #ffd6e7;
        transition: all 0.3s ease;
      }
      form:hover {
        transform: translateY(-3px);
        box-shadow: 0 0 20px rgba(255,192,203,0.6);
      }
      input {
        margin: 10px;
        padding: 10px;
        width: 130px;
        border-radius: 10px;
        border: 2px solid #ffcce0;
        text-align: center;
        outline: none;
        transition: all 0.2s;
        font-size: 14px;
      }
      input:focus {
        border-color: #ff99c8;
        box-shadow: 0 0 8px rgba(255,153,200,0.4);
      }
      button {
        padding: 10px 25px;
        background: #ffb6c1;
        color: white;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        font-size: 15px;
        transition: background 0.3s;
      }
      button:hover {
        background: #ff8fab;
      }
      h1 {
        color: #ff5d8f;
        font-size: 28px;
        margin-bottom: 25px;
      }
      h2 {
        color: #ff7aa2;
        margin-top: 20px;
        font-size: 22px;
      }
      p {
        color: #444;
        font-size: 16px;
      }
      .result-box {
        background: #fff0f5;
        display: inline-block;
        margin-top: 20px;
        padding: 15px 30px;
        border-radius: 15px;
        border: 1px solid #ffc1cc;
        box-shadow: 0 0 8px rgba(255,182,193,0.3);
      }
      .error {
        color: #ff4d6d;
        font-weight: bold;
        margin-top: 15px;
      }

  body
    h1  덧셈 & 뺄셈 계산기 
    form(action="/calculate" method="POST")
      input(type="text" name="num1" placeholder="첫 번째 숫자")
      input(type="text" name="num2" placeholder="두 번째 숫자")
      br
      button(type="submit") ✨ 계산하기 ✨

    if error
      p.error ⚠️ #{error}

    if result
      .result-box
        h2 🎀 결과 🎀
        p #{num1} + #{num2} = <b>#{result.sum}</b>
        p #{num1} - #{num2} = <b>#{result.diff}</b>